# Github Pages Cleaned Structure

This repo has been restructured for clarity:

- Main site pages (index, about, contact, 404) at root.
- Projects organized into `projects/nlp`, `projects/ml`, `projects/cv`, `projects/evo-algo`.
- Assets (CSS, JS, images, fonts) stored under `assets/`.
- System files like `.DS_Store` and `__MACOSX` removed.

